global
  log stdout format raw local0
  master-worker
  stats socket /var/run/haproxy/admin.sock mode 666 level admin expose-fd listeners

defaults
  mode http
  timeout connect 5s
  timeout client 30s
  timeout server 30s

# BEGIN LBM MANAGED CONFIGURATIONS
# END LBM MANAGED CONFIGURATIONS
